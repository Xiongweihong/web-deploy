name: Temporary Samsung Next AI portfolio extraction

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  extract:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Install extraction dependencies
        run: python -m pip install --disable-pip-version-check requests beautifulsoup4 lxml
      - name: Run evidence-first extraction
        run: python scripts/temporary_samsungnext_ai_extraction.py
      - name: Upload extraction evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: samsungnext-ai-portfolio-evidence
          path: artifact
          if-no-files-found: warn
          retention-days: 30
